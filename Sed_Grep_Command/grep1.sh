#This program outputs only those lines that contain the word 'the'. The search should be case sensitive. Here -w is used so that we get only the word "the" and not the words that contain the substring "the".
grep -w "the"
