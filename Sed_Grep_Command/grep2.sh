#This program outputs only those lines that contain the word 'the'. The search should NOT be case sensitive(-i provides this functionality). Here as in the previous example we need to match only the word "the" and not the words that contain the substring "the".
grep -iw "the"
